<div class="container-fluid col-md-8 col-md-offset-2">

    <!--<div ng-repeat="post in posts | orderBy:'-upvotes'">-->
        <!--<span class="glyphicon glyphicon-thumbs-up" ng-click="incrementUpvotes(post)"></span>-->
        <!--{{post.upvotes}}-->

        <!--&lt;!&ndash; ng-hide hides elements when angular expression evaluates to true &ndash;&gt;-->
        <!--<span style="font-size:20px; margin-left:10px;">-->
        <!--<a ng-show="post.link" href="{{post.link}}">-->
        <!--{{post.title}}-->
        <!--</a>-->

        <!--<span ng-hide="post.link">-->
        <!--{{post.title}}-->
        <!--</span>-->

        <!--<span ng-show="post.author">-->
        <!--posted by <a href="#/users/{{post.author}}">{{post.author}}</a>-->
        <!--</span>-->

        <!--<span>-->
        <!--&lt;!&ndash; When iterating over an array (posts), ng-repeat makes $index variable available along with each-->
        <!--item in the array &ndash;&gt;-->
        <!--<a href="#/posts/{{post._id}}">Comments</a>-->
        <!--</span>-->
    <!--</span>-->
    <!--</div>-->

    <hr />
    
    <form ng-submit="addPost()" ng-show="isLoggedIn()" style="margin-top:30px">
        <div class="row">
            <div class="col-md-10">
                <span class="form-group">
                    <input type="text"
                           class="form-control"
                           placeholder="Start a new discussion!"
                           ng-model="title">
                </span>
            </div>
            <div class="col-md-2 text-right">
                <button type="submit" class="btn btn-primary">Post</button>
            </div>
        </div>
    </form>

    <div ng-hide="isLoggedIn()">
        <h3>Please kindly login to join the discussion</h3>
    </div>

</div>